using UnityEngine;

public class Exercicio6 : MonoBehaviour
{
    public int idade;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("A sua idade é: " + idade);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
