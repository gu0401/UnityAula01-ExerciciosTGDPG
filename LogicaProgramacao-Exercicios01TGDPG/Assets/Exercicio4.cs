using UnityEngine;

public class Exercicio4 : MonoBehaviour
{
    int moedas = 50;
    int jogadores = 5;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print(moedas / jogadores);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
