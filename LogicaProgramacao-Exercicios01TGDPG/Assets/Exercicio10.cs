using UnityEngine;

public class Exercicio10 : MonoBehaviour
{
    float salario = 1000f;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log(salario + (salario * 0.10));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
